import * as React from "react";
import { CalculationTable } from "./CalculationTable";

export const Calculations = () => {
    // useEffect to get data
    return(
        <div>
            {
                // TODO : Search, Filter, New Calulation
            }
            <CalculationTable />
        </div>
    );
}